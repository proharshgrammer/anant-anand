---
phase: 01-admin-panel-foundation-auth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [package.json, middleware.ts, lib/supabase/client.ts, lib/supabase/server.ts, app/admin/login/page.tsx, next.config.js, tailwind.config.ts, tsconfig.json]
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03]
user_setup:
  - service: supabase
    why: "Requires Supabase Project credentials for database access"
    env_vars:
      - name: NEXT_PUBLIC_SUPABASE_URL
        source: "Supabase Settings -> API -> Project URL"
      - name: NEXT_PUBLIC_SUPABASE_ANON_KEY
        source: "Supabase Settings -> API -> anon public key"
      - name: SUPABASE_SERVICE_ROLE_KEY
        source: "Supabase Settings -> API -> service_role key (secret)"

must_haves:
  truths:
    - "Server runs and serves /admin/login page"
    - "Unauthenticated navigation to /admin/dashboard is blocked by middleware and redirected to /admin/login"
  artifacts:
    - path: "middleware.ts"
      provides: "Next.js routing auth guard"
    - path: "lib/supabase/client.ts"
      provides: "Supabase browser client helper"
    - path: "lib/supabase/server.ts"
      provides: "Supabase server client helper"
    - path: "app/admin/login/page.tsx"
      provides: "Login UI view"
---

<objective>
Bootstrap the Next.js 14 project, install all base dependencies, configure Supabase browser and server clients, and implement the middleware auth guard and admin login page.

Purpose: Create the foundation for the admin panel security and workspace.
Output: Initialized workspace, client utilities, route guard, and Login view.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-admin-panel-foundation-auth/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bootstrap Next.js 14 project scaffold</name>
  <files>package.json, tsconfig.json, next.config.js, tailwind.config.ts</files>
  <action>Initialize Next.js 14 App Router project in the current directory with TypeScript and Tailwind CSS using force overrides if needed. Configure package.json with scripts and install primary dependencies: @supabase/supabase-js, @supabase/auth-helpers-nextjs, react-hook-form, @hookform/resolvers, zod, lucide-react, clsx, tailwind-merge, and required devDependencies.</action>
  <verify>npm run build succeeds</verify>
  <done>package.json contains Next.js 14 and Supabase dependencies, and typescript build passes</done>
</task>

<task type="auto">
  <name>Task 2: Configure Supabase browser and server clients</name>
  <files>lib/supabase/client.ts, lib/supabase/server.ts</files>
  <action>Create lib/supabase/client.ts exporting a browser-side Supabase client using createClientComponentClient from @supabase/auth-helpers-nextjs. Create lib/supabase/server.ts exporting a server-side client utilizing createServerComponentClient from @supabase/auth-helpers-nextjs that parses headers/cookies.</action>
  <verify>tsc --noEmit passes</verify>
  <done>Supabase client helpers successfully export clientComponentClient and serverComponentClient</done>
</task>

<task type="auto">
  <name>Task 3: Implement route guard middleware</name>
  <files>middleware.ts</files>
  <action>Create middleware.ts checking session state using createMiddlewareClient from @supabase/auth-helpers-nextjs. Intercept any requests starting with /admin (excluding /admin/login) and redirect unauthenticated traffic to /admin/login. If a session exists and the user tries to access /admin/login, redirect them to /admin/dashboard.</action>
  <verify>tsc --noEmit passes</verify>
  <done>middleware.ts correctly filters /admin routes based on cookie sessions</done>
</task>

<task type="auto">
  <name>Task 4: Build Admin Login Page</name>
  <files>app/admin/login/page.tsx</files>
  <action>Create app/admin/login/page.tsx with a styled email/password login form using shadcn/ui styles. Wire up the form to submit to Supabase Auth login. On success, redirect the user to /admin/dashboard. Include validation (email format, password length) and render clear error banners on auth failure.</action>
  <verify>tsc --noEmit passes</verify>
  <done>Login page UI works and hooks into Supabase authentication</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run build succeeds
- [ ] tsconfig/ESLint rules pass without errors
</verification>

<success_criteria>
- Project structure initialized correctly.
- Admin authentication utilities and route guards operate without runtime errors.
</success_criteria>

<output>
After completion, create `.planning/phases/01-admin-panel-foundation-auth/01-01-SUMMARY.md`
</output>
