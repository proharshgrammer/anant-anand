---
phase: 01-admin-panel-foundation-auth
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: [app/admin/dashboard/page.tsx, app/admin/settings/page.tsx, components/admin/AdminNav.tsx, components/admin/EnquiryTable.tsx, lib/supabase/queries/settings.ts]
autonomous: true
requirements: [CMSM-01, CMSM-07]
must_haves:
  truths:
    - "Authenticated admin can view /admin/dashboard showing summary cards and recent enquiries"
    - "Admin can update site settings (phone, social URLs) via /admin/settings and save changes to DB"
  artifacts:
    - path: "components/admin/AdminNav.tsx"
      provides: "Layout sidebar menu shell"
    - path: "app/admin/dashboard/page.tsx"
      provides: "Dashboard overview interface"
    - path: "app/admin/settings/page.tsx"
      provides: "Site Settings edit screen"
    - path: "lib/supabase/queries/settings.ts"
      provides: "Supabase Settings DB query utility"
---

<objective>
Build the admin panel navigation sidebar, the primary Dashboard landing showing key statistics and the recent enquiries table, and the Site Settings configuration editor page.

Purpose: Provide the baseline content management controls for the agency owner.
Output: Sidebar layouts, dashboard widgets, and site setting forms.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-admin-panel-foundation-auth/01-CONTEXT.md
@.planning/phases/01-admin-panel-foundation-auth/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Admin Navigation Sidebar Layout</name>
  <files>components/admin/AdminNav.tsx</files>
  <action>Create a reusable AdminNav sidebar layout component. Render navigation items linking to Dashboard (/admin/dashboard), Tours (/admin/tours), Blogs (/admin/blog), Enquiries (/admin/enquiries), and Settings (/admin/settings). Use standard Lucide icons, implement responsive toggles for mobile viewports, and include a Log Out button executing Supabase signOut.</action>
  <verify>tsc --noEmit passes</verify>
  <done>Sidebar renders properly and handles mobile viewport collapse responsive triggers</done>
</task>

<task type="auto">
  <name>Task 2: Build Database Query for Site Settings</name>
  <files>lib/supabase/queries/settings.ts</files>
  <action>Create lib/supabase/queries/settings.ts containing getSettings() (fetching settings row id=1) and updateSettings() (updating row id=1). Ensure methods are typed according to the Supabase client schemas.</action>
  <verify>tsc --noEmit passes</verify>
  <done>CRUD queries for site settings are exported and type-safe</done>
</task>

<task type="auto">
  <name>Task 3: Implement Dashboard overview UI and Enquiries list</name>
  <files>app/admin/dashboard/page.tsx, components/admin/EnquiryTable.tsx</files>
  <action>Build app/admin/dashboard/page.tsx wrapped in AdminNav. Display four summary statistic cards: Total Tours, Published Tours, Blog Posts, and Enquiries. Fetch the 10 most recent records from Supabase enquiries table and render them inside EnquiryTable.tsx with basic sorting, date formatters, and status labels.</action>
  <verify>tsc --noEmit passes</verify>
  <done>Dashboard renders stats widgets and tabular recent enquiries</done>
</task>

<task type="auto">
  <name>Task 4: Build Site Settings manager page</name>
  <files>app/admin/settings/page.tsx</files>
  <action>Build app/admin/settings/page.tsx with a comprehensive React Hook Form. Display fields grouped by category: Agency Info (name, email, phone, address), Homepage Hero (headline, subheadline, background URL), Stats Counters (completed tours, years, destinations, families), Social Links, and SEO defaults. Fetch current values on mount and submit changes to Supabase settings using updateSettings().</action>
  <verify>tsc --noEmit passes</verify>
  <done>Settings forms retrieve, validate, and successfully persist settings data</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run build succeeds
- [ ] Ensure all forms are properly reset on successful save toasts
</verification>

<success_criteria>
- Admin sidebar navigates to all sub-pages.
- Settings forms fetch and write data from the Supabase site_settings schema.
</success_criteria>

<output>
After completion, create `.planning/phases/01-admin-panel-foundation-auth/01-02-SUMMARY.md`
</output>
