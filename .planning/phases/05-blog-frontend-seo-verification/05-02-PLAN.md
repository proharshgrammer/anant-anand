---
phase: 05-blog-frontend-seo-verification
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - app/contact/page.tsx
  - app/about/page.tsx
  - app/privacy/page.tsx
  - app/terms/page.tsx
  - components/public/Footer.tsx
  - app/page.tsx
  - app/tours/[slug]/page.tsx
  - next-sitemap.config.js
  - package.json
autonomous: true
requirements:
  - PUBL-07
  - SEOP-01
  - SEOP-02
  - SEOP-03
  - SEOP-04
must_haves:
  truths:
    - "Contact page shows office information (address, phone, email) and a working enquiry form"
    - "About page exists with company description and brand story"
    - "Privacy Policy and Terms of Use pages exist with legal content and noindex meta tags"
    - "Footer has links to Privacy Policy and Terms of Use pages"
    - "Tour detail pages include TouristTrip JSON-LD <script> tag"
    - "Homepage includes LocalBusiness JSON-LD <script> tag"
    - "Blog detail pages include Article JSON-LD <script> tag in head"
    - "sitemap.xml is generated during build containing public page URLs"
    - "robots.txt is generated during build disallowing /admin"
    - "Mobile PageSpeed score >= 85"
  artifacts:
    - path: app/contact/page.tsx
      provides: Contact page with office info and EnquiryForm
      metadata: static, title 'Get in Touch'
    - path: app/about/page.tsx
      provides: About page with company info
      metadata: static, title 'About'
    - path: app/privacy/page.tsx
      provides: Privacy Policy page
      metadata: static, noindex
    - path: app/terms/page.tsx
      provides: Terms of Use page
      metadata: static, noindex
    - path: components/public/Footer.tsx
      provides: Modified — added Privacy Policy and Terms of Use links
    - path: app/tours/[slug]/page.tsx
      provides: Modified — added TouristTrip JSON-LD inline script
    - path: app/page.tsx
      provides: Modified — added LocalBusiness JSON-LD inline script
    - path: next-sitemap.config.js
      provides: Sitemap and robots.txt generation config
      exports: IConfig with siteUrl, generateRobotsTxt, exclude patterns
    - path: package.json
      provides: Modified — added next-sitemap dependency and postbuild script
  key_links:
    - from: app/contact/page.tsx
      to: components/public/EnquiryForm.tsx
      via: "import EnquiryForm from '@/components/public/EnquiryForm'"
      pattern: "<EnquiryForm sourcePage=\"/contact\" />"
    - from: components/public/Footer.tsx
      to: app/privacy/page.tsx
      via: Link href="/privacy"
    - from: components/public/Footer.tsx
      to: app/terms/page.tsx
      via: Link href="/terms"
    - from: app/tours/[slug]/page.tsx
      to: JSON-LD
      via: Inline <script type="application/ld+json"> for TouristTrip
      pattern: "'@type': 'TouristTrip'"
    - from: app/page.tsx
      to: JSON-LD
      via: Inline <script type="application/ld+json"> for LocalBusiness
      pattern: "'@type': 'LocalBusiness'"
    - from: next-sitemap.config.js
      to: package.json
      via: npm run postbuild
      pattern: "postbuild"
---

<objective>
**As a** site owner, **I want to** ensure every page has proper SEO metadata, structured data for search engines, a sitemap for indexing, and professionally presented static informational pages, **so that** the website ranks well on organic search and establishes trust with potential customers.

This plan delivers static/legal pages (Contact, About, Privacy Policy, Terms of Use), adds JSON-LD structured data to all relevant page types, configures automated sitemap generation, and runs PageSpeed optimization to achieve >= 85 mobile score.

Purpose: Fulfill PUBL-07 (static pages), SEOP-01 (metadata coverage), SEOP-02 (JSON-LD schemas), SEOP-03 (sitemap/robots.txt), SEOP-04 (PageSpeed >= 85).
Output: 4 new page files, 1 new config file, 4 modified files. Routes at `/contact`, `/about`, `/privacy`, `/terms`.
</objective>

<execution_context>
@C:/Users/Harsh/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Harsh/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/PROJECT.md
@.planning/phases/05-blog-frontend-seo-verification/05-CONTEXT.md
@.planning/phases/05-blog-frontend-seo-verification/05-UI-SPEC.md
@.planning/phases/05-blog-frontend-seo-verification/05-PATTERNS.md
@.planning/phases/05-blog-frontend-seo-verification/05-01-PLAN.md

<interfaces>
<!-- Key contracts for existing components and query functions used in this plan -->

From app/actions/enquiry.ts — Server action signature:
```
export async function submitEnquiry(data: EnquiryFormData): Promise<EnquiryActionResult>
// EnquiryFormData: { name, email?, phone, message?, source_page?, tour_name?, tour_id? }
// EnquiryActionResult: { success: true } | { success: false; error: string }
```

From components/public/EnquiryForm.tsx — Props interface:
```
interface EnquiryFormProps {
  tourName?: string;
  tourId?: string;
  sourcePage?: string;
  onSuccess?: () => void;
}
// Uses: submitEnquiry server action, react-hook-form + zod
```

From app/tours/[slug]/page.tsx — Existing pattern for JSON-LD insertion:
Uses `createServerClient()` + `getTourBySlug(supabase, params.slug)`. 
Renders main content then returns `<main>...</main>` with `EnquiryPopup` at the end.
JSON-LD `<script>` should be placed before the `<main>` element (inside the fragment).

From app/page.tsx — Existing pattern for JSON-LD insertion:
Static server component with sections. JSON-LD `<script>` goes before `<div className="min-h-screen">`.

From types/index.ts — SiteSettings interface (for contact page office data):
```
interface SiteSettings {
  agency_name, tagline, phone, whatsapp_number, email, address,
  map_embed_url, social_facebook, social_instagram, social_youtube, ...
}
```
Note: Per D-15, static pages are hardcoded, not fetched from Supabase.
Per D-14, contact page uses hardcoded office information.

From app/destinations/page.tsx — Static server component pattern (exact analog for legal pages):
```
export const metadata: Metadata = { title: '...', description: '...' };
export default function Page() {
  return (<div className="min-h-screen bg-gray-50 pt-16 pb-24">
    <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">...</div>
  </div>);
}
```

From components/public/Footer.tsx — Current Quick Links structure (app/blog link already exists):
```
<div> {/* Quick Links column */}
  <h3 className="text-sm font-semibold text-white tracking-wider uppercase mb-4">Quick Links</h3>
  <ul className="space-y-3">
    <li><Link href="/">Home</Link></li>
    <li><Link href="/destinations">Destinations</Link></li>
    <li><Link href="/tours">Tours</Link></li>
    <li><Link href="/blog">Blog</Link></li>
    <li><Link href="/contact">Contact</Link></li>
  </ul>
</div>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create static pages (Contact, About, Privacy Policy, Terms of Use) and update Footer links</name>
  <files>
    app/contact/page.tsx
    app/about/page.tsx
    app/privacy/page.tsx
    app/terms/page.tsx
    components/public/Footer.tsx
  </files>
  <read_first>
    app/destinations/page.tsx — Exact analog for static server component pattern
    components/public/EnquiryForm.tsx — EnquiryFormProps interface, reuse on contact page
    app/actions/enquiry.ts — Server action signatures for form submission
    components/public/Footer.tsx — Current link structure; add privacy/terms links
    05-UI-SPEC.md — Contact page layout (two-column grid), About/Privacy/Terms layout (single column centered, prose)
    05-CONTEXT.md — D-14 (contact: static info + EnquiryForm), D-15 (about/privacy/terms: hardcoded)
  </read_first>
  <action>
    **1a — Create app/contact/page.tsx:**
    Server component (no 'use client'). Export static `metadata: Metadata`.
    
    Metadata block:
    ```
    title: 'Get in Touch',
    description: 'Contact Anant Anand Tour Packages. Call, message, or visit us for curated travel experiences across India.'
    ```
    
    Default export function. Page layout (per UI-SPEC):
    - `<main className="min-h-screen bg-gray-50 pt-16 pb-24">`
    - `<div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">`
    - `<div className="text-center mb-16">` with `h1` "Get in Touch" and `<p>` subheading from copywriting contract
    - Two-column grid: `<div className="grid grid-cols-1 lg:grid-cols-2 gap-12">`
    
    **Left column:**
    - Office information card with `bg-white rounded-2xl border border-gray-100 shadow-sm p-8`
    - Hardcoded info (per D-15, not fetched from Supabase):
      * Address with MapPin icon: "123 Ganga Vihar, Rishikesh, Uttarakhand 249201, India"
      * Phone with Phone icon: "+91 123 456 7890" (tel link)
      * Email with Mail icon: "namaste@anantanand.com" (mailto link)
      * Hours with Clock icon: "Mon–Sat, 9:00 AM – 7:00 PM"
    - Each info item: `flex items-start space-x-4` with icon container and text
    - After office info, render `<EnquiryForm sourcePage="/contact" />` (reuse existing EnquiryForm component per D-14)
    
    **Right column:**
    - Embedded map. Use a simple approach: create a `div` container with `w-full h-[450px] rounded-2xl overflow-hidden shadow-sm border border-gray-100` and include an `<iframe>` pointing to an OpenStreetMap embed URL for Rishikesh area:
      `<iframe src="https://www.openstreetmap.org/export/embed.html?bbox=78.28,30.08,78.33,30.14&layer=mapnik&marker=30.11,78.30" width="100%" height="100%" style="border:0;" allowfullscreen loading="lazy" referrerPolicy="no-referrer-when-downgrade"></iframe>`
    - Add attribution text below the iframe: `<p className="text-xs text-gray-400 mt-2 text-center">© OpenStreetMap contributors</p>`

    **1b — Create app/about/page.tsx:**
    Server component. Export static `metadata: Metadata`:
    ```
    { title: 'About', description: 'Learn about Anant Anand Tour Packages — our story, mission, and commitment to age-group based travel experiences.' }
    ```
    
    Default export function. Layout (per UI-SPEC):
    - `<main className="min-h-screen bg-gray-50 pt-16 pb-24">`
    - `<div className="max-w-3xl mx-auto px-4 sm:px-6 lg:px-8 py-16">`
    - `<h1 className="text-3xl font-semibold text-gray-900 font-display text-center mb-8">` "About Anant Anand Tour Packages"
    - Content in `<article className="prose prose-lg max-w-none">` with hardcoded sections:
      * "Our Story" section — 2-3 paragraphs about the company's founding and mission
      * "What We Offer" section — description of age-group based travel curation
      * "Our Commitment" section — safety, quality, customer service values
      * All content is hardcoded prose text (per D-15). Write natural, informative copy that reflects a professional travel agency.

    **1c — Create app/privacy/page.tsx:**
    Server component. Export static `metadata: Metadata`:
    ```
    {
      title: 'Privacy Policy',
      description: 'Privacy Policy for Anant Anand Tour Packages.',
      robots: { index: false, follow: true },
    }
    ```
    
    Default export. Same layout as About page (centered single column, prose). Hardcoded standard privacy policy content covering:
    - Information we collect (name, phone, email, travel preferences)
    - How we use your information (respond to enquiries, improve services)
    - Data sharing (we do not sell personal data)
    - Data retention and security
    - Contact information for privacy concerns
    - Last updated date
    
    **1d — Create app/terms/page.tsx:**
    Server component. Export static `metadata: Metadata`:
    ```
    {
      title: 'Terms of Use',
      description: 'Terms of Use for Anant Anand Tour Packages.',
      robots: { index: false, follow: true },
    }
    ```
    
    Default export. Same layout as About page. Hardcoded standard terms of use covering:
    - Booking and payment terms (for informational purposes; no online payment)
    - Cancellation and refund policy
    - User responsibilities
    - Limitation of liability
    - Governing law
    - Contact information
    
    **1e — Update components/public/Footer.tsx:**
    Add two new links to the Quick Links column (per UI-SPEC suggestion):
    After the existing Contact link (`<li><Link href="/contact">Contact</Link></li>`), add:
    - `<li><Link href="/privacy" className="...">Privacy Policy</Link></li>`
    - `<li><Link href="/terms" className="...">Terms of Use</Link></li>`
    
    Use the same `text-sm text-gray-400 hover:text-saffron-500 transition-colors` class pattern as the other footer links.

  </action>
  <verify>
    <automated>npm run build 2>&1 | Select-String -Pattern "error" -NotMatch -CaseSensitive -SimpleMatch | Select-String -Pattern "Error" -NotMatch -CaseSensitive | Select-Object -First 5</automated>
  </verify>
  <acceptance_criteria>
    <source-assertion>`app/contact/page.tsx` exists with static `metadata` export containing `title: 'Get in Touch'`</source-assertion>
    <source-assertion>`app/contact/page.tsx` imports `EnquiryForm` from `@/components/public/EnquiryForm` and renders it with `sourcePage="/contact"`</source-assertion>
    <source-assertion>`app/about/page.tsx` exists with static metadata and hardcoded prose content</source-assertion>
    <source-assertion>`app/privacy/page.tsx` exists with `metadata.robots.index === false`</source-assertion>
    <source-assertion>`app/terms/page.tsx` exists with `metadata.robots.index === false`</source-assertion>
    <source-assertion>`components/public/Footer.tsx` contains `<Link href="/privacy">` and `<Link href="/terms">` in the Quick Links list</source-assertion>
    <behavior-assertion>Visiting /contact shows a two-column layout on desktop with office info, EnquiryForm, and map embed</behavior-assertion>
  </acceptance_criteria>
  <done>
    All 4 static pages render correctly with proper metadata. Contact page includes office info and functioning EnquiryForm. Privacy and Terms pages have noindex. Footer links updated.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add JSON-LD structured data to existing pages (TouristTrip, LocalBusiness, Article)</name>
  <files>
    app/tours/[slug]/page.tsx
    app/page.tsx
  </files>
  <read_first>
    app/tours/[slug]/page.tsx — Current page; add TouristTrip JSON-LD before main element
    app/page.tsx — Current page; add LocalBusiness JSON-LD before main content
    05-PATTERNS.md — JSON-LD template for TouristTrip and Article (Article already added in 05-01 Task 3)
    05-CONTEXT.md — D-10 (manual <script> tags), D-11 (schema types: TouristTrip, Article, LocalBusiness)
    types/index.ts — Tour interface (for TouristTrip fields), SiteSettings (for LocalBusiness fields)
  </read_first>
  <action>
    **2a — Add TouristTrip JSON-LD to app/tours/[slug]/page.tsx:**
    In the `TourDetailPage` component, after `const tour = await getTourBySlug(...)` and before the `return` statement, construct a JSON-LD object with these minimum fields:
    ```
    {
      '@context': 'https://schema.org',
      '@type': 'TouristTrip',
      name: tour.title,
      description: tour.short_description || tour.description || undefined,
      image: tour.hero_image || (tour.images?.[0]) || undefined,
    }
    ```
    
    Wrap the existing returned JSX in a React Fragment (`<>...</>`) if it isn't already. Add the JSON-LD `<script>` tag at the top of the fragment (before the `<main>` element):
    ```
    <script
      type="application/ld+json"
      dangerouslySetInnerHTML={{ __html: JSON.stringify(jsonLd) }}
    />
    ```
    
    Also add an `Article` JSON-LD? No — D-11 says TouristTrip for Tour Detail, not Article. Just TouristTrip.
    Note the existing `generateMetadata` already handles meta tags per SEOP-01. This task only adds the JSON-LD schema script.

    **2b — Add LocalBusiness JSON-LD to app/page.tsx:**
    In the `HomePage` component, before the `return` statement, construct:
    ```
    {
      '@context': 'https://schema.org',
      '@type': 'LocalBusiness',
      name: 'Anant Anand Tour Packages',
      description: 'Curated travel experiences tailored to every age group — Senior Pilgrims, Families, Youth, and School Groups.',
      url: 'https://anantanandtourpackages.in',
      telephone: '+91 123 456 7890',
      email: 'namaste@anantanand.com',
      address: {
        '@type': 'PostalAddress',
        streetAddress: '123 Ganga Vihar',
        addressLocality: 'Rishikesh',
        addressRegion: 'Uttarakhand',
        postalCode: '249201',
        addressCountry: 'IN',
      },
    }
    ```
    
    Wrap the returned JSX in a Fragment and prepend the `<script type="application/ld+json">` tag with the LocalBusiness schema, exactly as done for TouristTrip above.
    
    **Note for verifier:** The Article JSON-LD for blog detail pages was already added in Plan 05-01 Task 3. This task confirms it exists but does not need to re-add it. After build, the blog detail page at `/blog/{slug}` should contain an Article schema script tag.

    **Metadata audit (SEOP-01):** After adding schemas, audit all public-facing page types for metadata coverage:
    - `/` (app/page.tsx) — ✅ has `export const metadata`
    - `/destinations` (app/destinations/page.tsx) — ✅ has `export const metadata`
    - `/tours/[slug]` (app/tours/[slug]/page.tsx) — ✅ has `generateMetadata`
    - `/blog` (app/blog/page.tsx) — ✅ created in 05-01 with metadata
    - `/blog/[slug]` (app/blog/[slug]/page.tsx) — ✅ created in 05-01 with generateMetadata
    - `/contact` (app/contact/page.tsx) — ✅ Task 1 adds static metadata
    - `/about` (app/about/page.tsx) — ✅ Task 1 adds static metadata
    - `/privacy` (app/privacy/page.tsx) — ✅ Task 1 adds static metadata
    - `/terms` (app/terms/page.tsx) — ✅ Task 1 adds static metadata
    
    No additional metadata changes needed. All page types have metadata coverage.
    
    If the build reveals any missing metadata on a page, add it following the pattern of either:
    - `export const metadata: Metadata = { ... }` for static pages
    - `export async function generateMetadata(...)` for dynamic pages
  </action>
  <verify>
    <automated>npm run build 2>&1 | Select-String -Pattern "error" -NotMatch -CaseSensitive -SimpleMatch | Select-String -Pattern "Error" -NotMatch -CaseSensitive | Select-Object -First 5</automated>
  </verify>
  <acceptance_criteria>
    <source-assertion>`app/tours/[slug]/page.tsx` contains `'@type': 'TouristTrip'` in a `<script type="application/ld+json">` tag</source-assertion>
    <source-assertion>`app/page.tsx` contains `'@type': 'LocalBusiness'` in a `<script type="application/ld+json">` tag</source-assertion>
    <source-assertion>`app/page.tsx` contains `'name': 'Anant Anand Tour Packages'` in the LocalBusiness schema</source-assertion>
    <source-assertion>`app/tours/[slug]/page.tsx` TouristTrip schema includes `name` and `description` fields</source-assertion>
    <test-command>npm run build; grep -r "'@type': 'TouristTrip'" .next/server/app/tours 2>/dev/null; if ($?) { Write-Output "TouristTrip schema found in build output" }</test-command>
  </acceptance_criteria>
  <done>
    TouristTrip JSON-LD added to all tour detail pages. LocalBusiness JSON-LD added to homepage. Article JSON-LD confirmed present on blog detail pages. All public page types have metadata coverage verified.
  </done>
</task>

<task type="auto">
  <name>Task 3: Configure next-sitemap, install dependency, and run PageSpeed optimization</name>
  <files>
    next-sitemap.config.js
    package.json
  </files>
  <read_first>
    package.json — Current scripts and dependencies; add postbuild script and next-sitemap
    05-PATTERNS.md — next-sitemap.config.js template, package.json modifications
    05-CONTEXT.md — D-12 (next-sitemap), D-13 (PageSpeed >= 85)
    05-UI-SPEC.md — Dependencies section confirming next-sitemap install
  </read_first>
  <action>
    **3a — Install next-sitemap:**
    Run `npm install next-sitemap` in the workspace root. This adds `next-sitemap` to `package.json` `dependencies` (let npm decide based on peer deps).

    **3b — Create next-sitemap.config.js:**
    At the project root (same level as `package.json`), create `next-sitemap.config.js`:
    ```
    /** @type {import('next-sitemap').IConfig} */
    module.exports = {
      siteUrl: process.env.SITE_URL || 'https://anantanandtourpackages.in',
      generateRobotsTxt: true,
      exclude: [
        '/admin/*',
        '/privacy',
        '/terms',
      ],
      robotsTxtOptions: {
        policies: [
          { userAgent: '*', allow: '/' },
          { userAgent: '*', disallow: '/admin' },
        ],
      },
      changefreq: 'weekly',
      priority: 0.7,
    };
    ```
    
    Notes:
    - `siteUrl` uses `process.env.SITE_URL` with fallback to the production URL
    - `generateRobotsTxt: true` automatically creates robots.txt (per SEOP-03)
    - Exclude admin routes (internal CMS), privacy, and terms (utility pages) from the sitemap
    - robots.txt disallows `/admin` for all user agents
    - The sitemap will automatically include all non-excluded static and ISR routes

    **3c — Update package.json scripts:**
    Add `"postbuild": "next-sitemap"` to the `scripts` section. After modification, the scripts block should contain:
    ```
    "scripts": {
      "dev": "next dev",
      "build": "next build",
      "postbuild": "next-sitemap",
      "start": "next start",
      "lint": "next lint"
    }
    ```
    
    **3d — Run build to verify sitemap generation:**
    Execute `npm run build`. After build completes, verify:
    - `public/sitemap.xml` exists (generated by next-sitemap postbuild script)
    - `public/robots.txt` exists
    - `public/sitemap.xml` contains URLs for public blog, tour, and destination pages
    - `public/sitemap.xml` does NOT contain `/admin/*` URLs
    - `public/robots.txt` contains `Disallow: /admin`
    
    **3e — PageSpeed optimization pass (SEOP-04):**
    After successful build, audit the following optimization areas. Only modify files if an issue is found:
    
    1. **Image optimization**: Scan all new pages (blog listing, blog detail, static pages) for `<img>` tags. Ensure each has explicit `width` and `height` attributes (or CSS-based aspect ratio containers) to prevent Cumulative Layout Shift (CLS). Add `loading="lazy"` to all below-the-fold images.
       - BlogCard images: Already have `h-48` CSS height. Verify the image container has explicit aspect ratio.
       - Blog detail hero image: Add `loading="lazy"` and ensure container has fixed height.
       - Related tours TourCard images: Already handled by TourCard component.
    
    2. **Font optimization**: Verify fonts are loaded with `display: 'swap'` (already in app/layout.tsx for Inter, Playfair, Poppins, Tiro). No changes needed unless build output shows font-related warnings.
    
    3. **ISR configuration**: Verify all new blog pages have `revalidate = 3600` (already planned in 05-01). Static pages don't need revalidation.
    
    4. **Build output verification**: Run `npm run build` and check the final output for:
       - No JavaScript errors during build
       - All routes are pre-rendered (static or ISR)
       - Sitemap generated correctly
    
    5. If any specific PageSpeed issues are identified from build warnings (e.g., "Image elements do not have explicit width and height"), fix them in the relevant component files. Add `width`/`height` attributes to `<img>` elements where missing, using the known display dimensions (e.g., for BlogCard images: width={800} height={450} for the 16:9 aspect ratio container).

    **3f — PageSpeed measurement verification:**
    Measure the result to confirm SEOP-04 (Mobile PageSpeed >= 85):
    - Run production build: `npm run build && npm run start` (start production server in background)
    - Run Lighthouse CI: `npx lighthouse http://localhost:3000 --output=json --chrome-flags="--headless" --only-categories=performance 2>$null | Select-String -Pattern '"score":\d+'` 
    - If Lighthouse CLI is not available or fails, add a manual checkpoint to the phase SUMMARY.md noting: `**MANUAL VERIFICATION REQUIRED**: Run PageSpeed Insights (pagespeed.web.dev) against production build to confirm Mobile score >= 85. Document score in SUMMARY.md.`
    - Record the achieved performance score in the phase SUMMARY.md under section `## PageSpeed Results`
  </action>
  <verify>
    <automated>
      npm run build; if ($LASTEXITCODE -eq 0) { 
        $sitemapExists = Test-Path -LiteralPath "public/sitemap.xml"
        $robotsExists = Test-Path -LiteralPath "public/robots.txt"
        if ($sitemapExists -and $robotsExists) { Write-Output "Sitemap and robots.txt generated successfully" }
        else { Write-Output "WARNING: sitemap.xml or robots.txt not found in public/" }
      }
    </automated>
  </verify>
  <acceptance_criteria>
    <source-assertion>`next-sitemap.config.js` exists at project root with `siteUrl`, `generateRobotsTxt: true`, and `exclude: ['/admin/*', '/privacy', '/terms']`</source-assertion>
    <source-assertion>`package.json` `scripts` contains `"postbuild": "next-sitemap"`</source-assertion>
    <source-assertion>`public/sitemap.xml` is generated after build and contains blog-related URLs</source-assertion>
    <source-assertion>`public/robots.txt` is generated and contains `Disallow: /admin`</source-assertion>
    <source-assertion>`public/sitemap.xml` does not contain `/admin/` in any URL entry</source-assertion>
    <test-command>`npm run build` exits with code 0</test-command>
    <test-command>`Test-Path -LiteralPath "public/sitemap.xml"` returns True</test-argument>
    <test-command>`Test-Path -LiteralPath "public/robots.txt"` returns True</test-argument>
  </acceptance_criteria>
  <done>
    next-sitemap installed and configured. Sitemap and robots.txt generated on build. PageSpeed optimization applied — images have lazy loading and explicit dimensions where needed. Build completes without errors.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → Contact form | User submits name, phone, email via EnquiryForm. Input crosses trust boundary at server action. |
| Build → Sitemap | Build-time generation of public sitemap — admin paths must be excluded. |
| Public → Static pages | Read-only browsing with no user input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Information Disclosure | Sitemap exposing admin paths | mitigate | Exclude `/admin/*` in next-sitemap.config.js (implemented in Task 3). robots.txt Disallow: /admin for crawler guidance. |
| T-05-04 | Tampering | XSS via JSON-LD `dangerouslySetInnerHTML` | accept | JSON-LD data is hardcoded literals (Task 2) or admin-authored content (Article schema in 05-01). No user input flows into JSON-LD stringification. |
| T-05-05 | Denial of Service | Large unoptimized images causing slow load times | mitigate | All images use `loading="lazy"`, explicit width/height attributes, and CSS aspect-ratio containers. ISR reduces server load. (SEOP-04) |
| T-05-SC | Tampering | npm install (`next-sitemap`) | mitigate | `next-sitemap` is a well-established package (2M+ weekly downloads, 2.5k+ GitHub stars). No slopcheck needed — [ASSUMED] per Policy A. |
</threat_model>

<verification>
- `npm run build` completes with exit code 0
- `public/sitemap.xml` and `public/robots.txt` generated
- All 4 static pages render at their respective routes without 404
- Contact page shows EnquiryForm component and office information
- Privacy and Terms pages have `<meta name="robots" content="noindex">` in HTML output
- Tour detail pages contain `@type: TouristTrip` in inline JSON-LD
- Homepage contains `@type: LocalBusiness` in inline JSON-LD
- Blog detail pages contain `@type: Article` in inline JSON-LD
</verification>

<success_criteria>
- `/contact` page displays office info, map embed, and functioning EnquiryForm
- `/about` page displays about copy with prose styling
- `/privacy` and `/terms` display legal content with noindex meta tags
- Footer has Privacy Policy and Terms of Use links
- All tour detail pages include TouristTrip JSON-LD in HTML source
- Homepage includes LocalBusiness JSON-LD in HTML source
- All blog detail pages include Article JSON-LD in HTML source
- `sitemap.xml` generated at build, contains public pages, excludes `/admin/*`
- `robots.txt` generated at build, disallows `/admin`
- Build completes without errors
</success_criteria>

<output>
Create `.planning/phases/05-blog-frontend-seo-verification/05-02-SUMMARY.md` when done
</output>
